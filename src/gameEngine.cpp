#include <SFML/Graphics.hpp>
#include <cstdlib>
#include <ctime>
#include <vector>
#include <iostream>


void updateGame(sf::RenderWindow &window, int timeStep, int tileSize,
    int windowWidth, int windowHeight, std::vector<Enemy> &stored_enemies){
        
    
    
          
}

// void drawEnemies(sf::RenderWindow &window, int timeStep, int tileSize,
//     int windowWidth, int windowHeight, std::vector<Enemy> &stored_enemies){
    
//     for (int i = 0; i < stored_enemies.size(); i++){
//         stored_enemies[i].moveEnemy(timeStep, window);
//     }
// }

// draw enemies on the first cycle of the game loop
// void drawElements(sf::RenderWindow &window, int timeStep, int tileSize,
//     int windowWidth, int windowHeight, std::vector<Enemy> &stored_enemies){
    
//     //draw tiles
//     drawTiles(window,tileSize,windowWidth,windowHeight);
//     //draw enemies
//     drawEnemies(window, timeStep, tileSize, windowWidth, windowHeight, stored_enemies);
//     //draw towers
//     drawTowers(window);
// }


